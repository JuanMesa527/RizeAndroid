<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:name=".RizeApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RizeAndroid">

        <activity
            android:name=".ui.HomepageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.SelectActivity" />
        <activity android:name=".ui.PositionGuideActivity" />
        <activity android:name=".ui.CameraActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.SummaryActivity" />
        <activity android:name=".ui.SessionHistoryDetailActivity" />
        <activity android:name=".ui.StatsHistoryActivity" />
        <activity android:name=".ui.VideoAnalysisActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.AboutActivity" />

    </application>
</manifest>
